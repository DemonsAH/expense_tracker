# 图片预处理指南（小票抠图 + 透视矫正）

> 用途：本文档给其他 Agent 使用。任何需要"从照片中裁出小票、矫正成长方形"再送 OCR 的
> 任务，直接复用 `小票V1.2\src\expense_tracker\preprocess.py` 的逻辑，不要重新设计算法。

## 1. 一句话总结

假设小票是**平放在黑色背景上**拍摄的。预处理模块用 **Otsu 阈值（色差/明暗对比）** 分离
出最亮连通域作为小票区域，用 `minAreaRect` 找最小外接矩形角点，再做**透视变换**矫正成
标准长方形，最后统一缩放 + 锐化，得到"小、正、干净"的 OCR 输入图。

## 2. 调用关系（在流水线中的位置）

```
pipeline.py: decode_image() → preprocess() → 送 OCR
```

- 入口：`preprocess.py::preprocess(image, target_width=1600)`，返回 `PreprocessResult`
  （含 `image`、`found_receipt`、`method`、`corners`、`info`）。
- `decode_image(bytes)`：网络上传的原始字节流解码成 BGR ndarray。
- `load_image(path)`：本地文件读取。
- CLI 自测：`python -m expense_tracker.cli test-ocr 小票.jpg ...` 会先打印
  `[preprocess] method=... found_receipt=... info=...`。
- 诊断记录：`diagnostics.py` 会把预处理后的图存到 run 目录 `preprocessed/` 供排错。

## 3. 算法流程（逐步骤）

### 步骤 1：灰度 + 高斯模糊

```python
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
blurred = cv2.GaussianBlur(gray, (5, 5), 0)
```

5×5 高斯核降噪，避免噪点干扰后续阈值。

### 步骤 2：Otsu 自动阈值（核心：用明暗差异找小票）

```python
_, thresh = cv2.threshold(blurred, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
```

Otsu 自动算最佳阈值，把图像分成"亮（小票纸面）"和"暗（黑色背景）"两块。
**这就是"色差/对比度找边框"的实现**——不需要指定颜色，只要背景比纸暗就分得开。

### 步骤 3：找最大连通域

```python
contours, _ = cv2.findContours(thresh, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
largest = max(contours, key=cv2.contourArea)
```

只取**最外层轮廓**（`RETR_EXTERNAL`），再取**面积最大**的那个当作小票。

### 步骤 4：面积占比合理性检查（防误判）

```python
area_ratio = cv2.contourArea(largest) / (h * w)
if area_ratio < MIN_RECEIPT_AREA_RATIO:  # 0.03，太亮太小 → 不是小票
    return _passthrough(...)
if area_ratio > MAX_BRIGHT_RATIO:        # 0.85，全图都亮 → 根本没有黑底
    return _passthrough(...)
```

| 常量 | 值 | 含义 |
|------|-----|------|
| `MIN_RECEIPT_AREA_RATIO` | 0.03 | 亮区必须 ≥ 3% 画面，否则忽略（可能只是噪点） |
| `MAX_BRIGHT_RATIO` | 0.85 | 亮区 > 85% 说明整图都是亮的，没有黑底，跳过抠图 |

### 步骤 5：最小外接矩形 + 角点排序

```python
rect = cv2.minAreaRect(largest)      # 旋转矩形 (center, (w,h), angle)
box = cv2.boxPoints(rect)            # 4 个角点，顺序任意
corners = _order_corners(box)        # 规整成 [TL, TR, BR, BL]
```

`_order_corners` 排序技巧：
- `s = x + y`：左上角 s 最小，右下角 s 最大；
- `d = x - y`：右上角 d 最大，左下角 d 最小。

### 步骤 6：透视变换矫正成矩形

```python
w_top  = |TR - TL|    w_bot = |BR - BL|
h_left = |BL - TL|    h_right = |BR - TR|
out_w = round(max(w_top, w_bot))    # 取上下宽度的较大者，抗透视误差
out_h = round(max(h_left, h_right))
# 退化保护：任一边 < 50px 视为无效
if out_w < 50 or out_h < 50: return _passthrough(...)

dst = [[0,0],[out_w-1,0],[out_w-1,out_h-1],[0,out_h-1]]   # 标准矩形
matrix = cv2.getPerspectiveTransform(corners, dst)
warped = cv2.warpPerspective(image, matrix, (out_w, out_h))
```

### 步骤 7：统一缩放 + 锐化

```python
warped = _fit_to_width(warped, target_width)   # 长边压到 1600px，INTER_AREA 缩
warped = _sharpen(warped)                       # unsharp mask 轻度锐化
```

- `_fit_to_width`：只缩不放；已 ≤ 1600px 则原样。
- `_sharpen`：`out = 1.6*img - 0.6*GaussianBlur(sigma=1.5)`（unsharp mask），帮助小字号清晰。

## 4. 兜底逻辑（passthrough）

当找不到可信的小票区域（无轮廓 / 亮区太小 / 全图皆亮 / 四边形退化），不报错，走透传：

```python
img = _fit_to_width(image, target_width)
img = _sharpen(img)
# method="passthrough", found_receipt=False, info={"reason": "..."}
```

这样**已经裁好的图片也能正常工作**，只是少了矫正。`found_receipt=False` 可用来统计
"黑底拍摄占比"（诊断报告里有 `preprocess_passthrough_count`）。

## 5. 为什么是 1600px（不要乱改）

- 1024px 实测**明显掉精度**（OCR README 记录）；
- prefill 只占 ~0.2s，大图几乎不增耗时（耗时大头是解码输出 token）；
- 所以维持 1600px，别为省算力降分辨率。

## 6. 关键参数速查

| 参数 | 位置 | 值 | 作用 |
|------|------|-----|------|
| `MIN_RECEIPT_AREA_RATIO` | 模块常量 | 0.03 | 小票最小面积占比 |
| `MAX_BRIGHT_RATIO` | 模块常量 | 0.85 | 全亮图判定阈值 |
| `TARGET_WIDTH` | 模块常量 | 1600 | 长边目标宽度 |
| 高斯核 | 阈值前 | (5,5) | 降噪 |
| 阈值法 | 二值化 | Otsu | 自动分亮/暗 |
| 轮廓模式 | findContours | RETR_EXTERNAL | 只取最外层 |
| 输出边保护 | 透视前 | ≥50px | 防退化四边形 |

## 7. 复用地雷

1. **前提是黑底**：浅色/白底上拍的小票会走 passthrough（`found_receipt=False`），不矫正。
2. **输入必须 BGR**：`decode_image`/`load_image` 读出来就是 BGR；直接接 OpenCV 图没问题，
   但如果是 RGB/别的通道序，`cvtColor(BGR2GRAY)` 结果会偏。
3. **返回的是矫正后的新图**：`PreprocessResult.image`，不是原图；OCR 只用这个。
4. **缩放用 INTER_AREA**（缩小专用插值），不要换成其他插值，否则小票文字会糊。
5. 矫正后小票是"竖直长方形"，但**不保证方向**（可能上下颠倒）——本系统 OCR 对方向不敏感，
   若你的下游依赖文字方向，需另加方向判断。
